# Phase 0 macOS verification

| | |
|---|---|
| Date | 2026-09-23 |
| Host | macOS 27.0 (Build 26A428), Apple Silicon (`arm64`) |
| Python | 3.10.0 (`.venv-mac`) |
| Branch verified | `origin/claude/p0-assurance-meta` @ `c912ef9` |
| Result | **4 of 6 checks PASS** (1 FAIL, 1 SKIPPED) |

No product code was changed. This file is the only artifact of the run.

---

## 1. Test suite — PASS

**Command:** `find . -name __pycache__ -type d -prune -exec rm -rf {} + && python -m pytest -q`

**Output (last line):**
```
681 passed, 3 skipped in 50.47s
```

**Skips (`pytest -rs`):**
```
SKIPPED [1] packages/assurance/tests/test_front_door.py:26: tomllib arrives in 3.11
SKIPPED [1] packages/assurance/tests/test_front_door.py:39: tomllib arrives in 3.11
SKIPPED [1] packages/deps/tests/test_manifest_kinds.py:188: no tomllib on this interpreter, so there is nothing to compare against
```

**Verdict:** PASS — no failures; the three skips are the Python 3.10 / `tomllib` gap (expected 684 passed on 3.11+; "a few skips are fine").

---

## 2. Type checks — FAIL

**Command (each of `core cli mcp budget authority deps`):**
`(cd packages/<pkg> && python -m mypy --strict assurance_<pkg>)`

| Package | Output |
|---|---|
| core | `Success: no issues found in 22 source files` |
| cli | `Success: no issues found in 10 source files` |
| mcp | `Success: no issues found in 6 source files` |
| budget | `Success: no issues found in 5 source files` |
| authority | `Success: no issues found in 6 source files` |
| deps | FAIL (exit 1) — see below |

**deps output:**
```
assurance_deps/manifest.py:531: error: Cannot find implementation or library stub for module named "tomllib"  [import-not-found]
assurance_deps/manifest.py:531: note: See https://mypy.readthedocs.io/en/stable/running_mypy.html#missing-imports
Found 1 error in 1 file (checked 9 source files)
```

**Verdict:** FAIL — five packages are clean; `assurance-deps` fails strict mypy on Python 3.10 because `tomllib` is 3.11+.

---

## 3. One command reaches every tool — PASS

**Commands:** `assurance --help`, `assurance authority --example`, `assurance budget --help`, `assurance deps --help`

**`assurance --help` (trimmed):** positional commands include `deps`, `budget`, `authority` (and `init`, `check`, `diff`, `pin`, `drift`). Exit 0.

**`assurance authority --example` (trimmed):**
```
1 of 3 tasks may proceed for the person who asked — 1 moved owner — 1 refused
...
These are three people in a built-in example, not your organisation.
```
Exit 0.

**`assurance budget --help`:** usage for `assurance-budget` with log path and caps. Exit 0.

**`assurance deps --help`:** usage for `assurance deps` with manifest / `--from` / `--lock` / `--offline`. Exit 0.

**Verdict:** PASS — `--help` lists `budget` and `authority`; each command ran.

---

## 4. MCP folder boundary — PASS

**Setup:**
```bash
mkdir -p ~/assurance-test/Reports && printf 'date,n\n2026-01-05,1\n' > ~/assurance-test/Reports/2026-01.csv
```

**Command:** the `list_dated_files` boundary script from the task brief.

**Output:**
```
'' -> OK, 1 period(s)
'/Users/ashwinthreddy/assurance-test/Reports' -> OK, 1 period(s)
'/Users/ashwinthreddy/assurance-test/reports' -> '/Users/ashwinthreddy/assurance-test/reports' is not inside a folder this server was granted. Granted: /Users/ashwinthreddy/assurance-test/Reports. The boundary is set in the MCP client's config (`--root`), not by a tool argument.
'/etc' -> '/etc' is not inside a folder this server was granted. Granted: /Users/ashwinthreddy/assurance-test/Reports. The boundary is set in the MCP client's config (`--root`), not by a tool argument.
'/' -> '/' is not inside a folder this server was granted. Granted: /Users/ashwinthreddy/assurance-test/Reports. The boundary is set in the MCP client's config (`--root`), not by a tool argument.
```

**Lower-case `reports` question:** refused (case-sensitive path match on macOS APFS default).

**Verdict:** PASS — empty and exact grant are OK; `/etc` and `/` refused; lower-case `reports` refused.

---

## 5. `pin` against real Claude Desktop config — PASS

**Config present:** `~/Library/Application Support/Claude/claude_desktop_config.json` exists.

**Shape (no secrets):** top-level keys are `coworkUserFilesPath`, `preferences`; **zero** `mcpServers` entries.

**Commands** (from `~/assurance-test/pin`):
```bash
assurance pin --save --config ~/Library/Application\ Support/Claude/claude_desktop_config.json
assurance pin --check --config ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

**`--save` output / exit:**
```
Using MCP config: /Users/<redacted>/Library/Application Support/Claude/claude_desktop_config.json
assurance: no stdio MCP servers to pin
```
`echo $?` → `2`

**`--check` output / exit:**
```
Using MCP config: /Users/<redacted>/Library/Application Support/Claude/claude_desktop_config.json
assurance: no pin file at /Users/<redacted>/assurance-test/pin/.assurance/mcp-pins.json — run `assurance pin --save` first
```
`echo $?` → `2`

**Verdict:** PASS — pin reached the real config on macOS; exit 2 is the correct response for a Claude Desktop config with no stdio MCP servers (nothing to pin, so nothing to check).

---

## 6. Zero-install path — SKIPPED

**Command:** `uvx --from ./packages/assurance assurance --help | head -3`

**Verdict:** SKIPPED — `uv` / `uvx` is not installed on this Mac (`command -v uv` and `command -v uvx` both empty; not under Homebrew or `~/.local/bin`).

---

## Summary

| # | Check | Result |
|---|---|---|
| 1 | pytest | PASS (681 passed, 3 skipped — tomllib / 3.10) |
| 2 | mypy --strict ×6 | FAIL (`assurance-deps` / `tomllib` on 3.10) |
| 3 | CLI front door | PASS |
| 4 | MCP folder boundary | PASS (lower-case `reports` refused) |
| 5 | pin vs Claude Desktop | PASS (exit 2, zero MCP servers in config) |
| 6 | `uvx` zero-install | SKIPPED (`uv` absent) |

**4 of 6 checks pass.** Failures and skips share one cause: this host’s Python is 3.10 (no stdlib `tomllib`) and `uv` is not installed. No product patches were made.
